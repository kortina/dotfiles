set noswapfile
set nobackup
set nowb
set nowritebackup
nmap <Leader>nt :NERDTreeToggle<CR>
" leader k to run focused spec in karma daemon
" start the daemon w `fd karma-focused-runner dashboard` (or web)
autocmd FileType javascript map <Leader>rk :call VimuxRunCommand("clear; CHROME_BIN=chromium-browser ./dev-scripts/karma-start-single-run-line-number.sh " . expand("%.") . ":" . line("."))<CR> 
